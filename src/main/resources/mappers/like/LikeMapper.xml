<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.kosa.backend.like.mapper.LikeMapper">

    <resultMap id="LikeResultMap" type="kr.or.kosa.backend.like.domain.Like">
        <id property="userId" column="USER_ID"/>
        <result property="referenceType" column="REFERENCE_TYPE"
                javaType="kr.or.kosa.backend.like.domain.ReferenceType"/>
        <result property="referenceId" column="REFERENCE_ID"/>
        <result property="createdAt" column="CREATED_AT"/>
    </resultMap>

    <select id="selectLike" resultMap="LikeResultMap">
        SELECT *
        FROM `LIKE`
        WHERE USER_ID = #{userId}
        AND REFERENCE_TYPE = #{referenceType}
        AND REFERENCE_ID = #{referenceId}
    </select>

    <insert id="insertLike">
        INSERT INTO `LIKE` (USER_ID, REFERENCE_TYPE, REFERENCE_ID, CREATED_AT)
        VALUES (#{userId}, #{referenceType}, #{referenceId}, NOW())
    </insert>

    <delete id="deleteLike">
        DELETE FROM `LIKE`
        WHERE USER_ID = #{userId}
        AND REFERENCE_TYPE = #{referenceType}
        AND REFERENCE_ID = #{referenceId}
    </delete>

    <delete id="deleteByReference">
        DELETE FROM `LIKE`
        WHERE REFERENCE_TYPE = #{referenceType}
        AND REFERENCE_ID = #{referenceId}
    </delete>

    <!-- 사용자가 좋아요 누른 ID 목록 조회 -->
    <select id="selectLikedReferenceIds" resultType="java.lang.Long">
        SELECT REFERENCE_ID
        FROM `LIKE`
        WHERE USER_ID = #{userId}
        AND REFERENCE_TYPE = #{referenceType}
        AND REFERENCE_ID IN
        <foreach collection="referenceIds" item="refId" open="(" separator="," close=")">
            #{refId}
        </foreach>
    </select>

</mapper>