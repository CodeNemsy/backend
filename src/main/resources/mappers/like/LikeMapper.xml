<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.kosa.backend.like.mapper.LikeMapper">

    <resultMap id="likeRecordResultMap" type="kr.or.kosa.backend.like.domain.LikeRecord">
        <id property="likeId" column="like_id"/>
        <result property="userId" column="user_id"/>
        <result property="referenceType" column="reference_type"
                typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result property="referenceId" column="reference_id"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <!-- 좋아요 추가 -->
    <insert id="insertLike" parameterType="kr.or.kosa.backend.like.domain.LikeRecord"
            useGeneratedKeys="true" keyProperty="likeId">
        INSERT INTO like_record (
        user_id,
        reference_type,
        reference_id,
        created_at
        ) VALUES (
        #{userId},
        #{referenceType},
        #{referenceId},
        NOW()
        )
    </insert>

    <!-- 좋아요 삭제 -->
    <delete id="deleteLike">
        DELETE FROM like_record
        WHERE user_id = #{userId}
        AND reference_type = #{referenceType}
        AND reference_id = #{referenceId}
    </delete>

    <!-- 좋아요 존재 여부 확인 -->
    <select id="selectLike" resultMap="likeRecordResultMap">
        SELECT *
        FROM like_record
        WHERE user_id = #{userId}
        AND reference_type = #{referenceType}
        AND reference_id = #{referenceId}
    </select>

    <!-- 사용자가 좋아요 누른 ID 목록 조회 -->
    <select id="selectLikedReferenceIds" resultType="long">
        SELECT reference_id
        FROM like_record
        WHERE user_id = #{userId}
        AND reference_type = #{referenceType}
        AND reference_id IN
        <foreach collection="referenceIds" item="refId" open="(" separator="," close=")">
            #{refId}
        </foreach>
    </select>

    <!-- 참조 대상의 모든 좋아요 삭제 -->
    <delete id="deleteByReference">
        DELETE FROM like_record
        WHERE reference_type = #{referenceType}
        AND reference_id = #{referenceId}
    </delete>

</mapper>